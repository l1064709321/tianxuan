# 天玄 TianXuan — 宪章(每日必读, 偏离即纠正)

本文件为项目最高优先级指令,每次会话自动加载。每天开工第一件事:对照本文件检查当前代码与方向,发现偏离立即纠正。详细设计与论文依据见 PLAN.md。

## 一、产品定义(用户拍板, 不可回退)
1. **真产品, 不是 demo**: 全局工作站 + 多神经协同必须是真实运行的机制。禁止用写死置信度/假输出的 stub 充当实现(demo 只能存在于 `src/station/demo.ts`, 不得进入真实路径)。
2. **多神经协同 = 领域专家型**: 按任务领域(text/vision/speech/world)路由到该领域优势神经; 神经链结构 = 感知神经 → 中央神经 → 输出神经; 所有神经经**共享工作空间 z(黑板)**协同, 神经间不得直接互调。
3. **执行顺序(用户拍板)**: 先把多神经协同做真 → 再引入语料库/向量库预训练。不得颠倒。
4. **全局工作站(Global Station)是中枢**: 注册中心(带证据门槛) + 黑板 z(唯一共享状态) + 分级执行器(MoD 式) + 健康监控 + 审计日志。

## 二、借鉴原则
5. **只借逻辑, 不抄代码**: 官方论文/开源项目仅作「机制如何形成」的依据与验证思路; 所有实现用天玄自研数值内核, 不引入外部模型/库依赖。
6. **官方依据(2026)**: GPT-5 实时路由(arXiv:2601.03267) / Genie 三神经(arXiv:2402.15391) / Titans(arXiv:2501.00663) / MoD(arXiv:2404.02258) / Anthropic 全局工作空间(arXiv:2607.15495) / Ignition Index(arXiv:2608.05160) / Worldscape-MoE(arXiv:2607.03964)。

## 三、证据门槛
7. 注册表神经 enabled 必须带 justification + 消融, 证明修复了基线某指标缺口; 没有证据不允许 enabled(SNN/STDP/STDA 在 CPU 文本世界无收益场景, 保持货架)。
8. 多神经协同是否真成立必须可量化: 用自研探针(借鉴 Ignition Index 思路)验证「慢语义神经点火、快动力学神经渐进」的分工, 数字说话。

## 四、沙箱纪律
9. 所有验证/训练/生成/实验环节一律在 `sandbox/` 内完成: 编译产物 `sandbox/dist`, 数据产物 `sandbox/data|checkpoints`; 不得触碰生产 `data/` 与根目录 `dist/`。
10. 正式模型进沙箱跑 = 只读复制(`data/checkpoints` → `sandbox/`), 生产文件永不修改。

## 五、每日检查清单(每天过一遍)
- [ ] 当前改动是否仍符合「先多神经 → 后语料库预训练」顺序?
- [ ] 是否有 stub/写死输出混进了真实路径?
- [ ] 多神经协同是否走共享 z(黑板), 神经间是否直接互调(禁止)?
- [ ] 所有验证是否都在 `sandbox/`, 生产 `data/` 是否未被触碰?
- [ ] 注册表新神经是否带 justification + 消融计划?
- [ ] 是否偏离官方依据(只借逻辑, 未抄代码/未引入外部依赖)?

执行: `bash scripts/daily-check.sh` 做机器可查项检查; 其余人工核对。
