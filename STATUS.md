# 天玄项目状态报告

**日期**: 2026-08-26
**状态**: 多神经协同架构重构中

---

## 一、项目概况

### 1.1 基本信息
- **项目根**: `/home/aidlux/tianxuan/`
- **用户要求**: 所有项目统一在此目录下，所有检索都在天玄项目下进行
- **框架**: TypeScript / Node.js (纯 CPU，零原生依赖)
- **核心**: 字符级语言模型，纯手写数值内核

### 1.2 目录结构
```
/home/aidlux/tianxuan/
├── src/ml/              # 源码目录
│   ├── train.ts         # 训练入口
│   ├── gru.ts           # CharGRU 模型（串行叠加版）
│   ├── mamba.ts         # SelectiveSSM（Mamba 内核）
│   ├── stdp.ts          # STDP 突触可塑性
│   ├── stda.ts          # STDA 阈值适应
│   ├── titans.ts        # Titans 记忆模块
│   ├── model.ts         # CharMLP（旧版 MLP）
│   ├── rng.ts           # 随机数生成
│   ├── tokenizer.ts     # 字符分词
│   ├── vectorstore.ts   # 向量存储
│   ├── corpus.ts        # 语料处理
│   ├── data.ts          # 数据加载
│   ├── engine.ts        # 引擎
│   ├── generate.ts      # 文本生成
│   └── watchdog.ts      # 看门狗
├── sandbox/dist/ml/     # 编译输出
├── sandbox/logs/        # 日志目录
├── sandbox/ck-full-multi/  # 检查点
├── scripts/             # 脚本目录
│   ├── bootstrap-resume.sh
│   ├── watch-train.sh
│   └── start-train.sh
├── src/station/         # 神经系统注册中心
│   ├── registry.ts      # 注册表
│   └── types.ts         # 类型定义
└── tsconfig.json        # outDir: sandbox/dist
```

---

## 二、当前架构问题

### 2.1 现有 CharGRU 架构
当前 `gru.ts` 是**串行叠加**结构，不是真正的多神经协同：

```
输入 → [Embed] → [GRU Cell 1] → [GRU Cell 2 / Mamba] → [Sparse Attn] → [CNN] → [输出头]
                depth=1          depth=2               depth=3           depth=4
```

**问题**:
1. 所有神经模块按固定顺序执行，没有动态路由
2. 没有 MoE（混合专家）机制
3. STDP/STDA 只是附加正则，不是核心架构
4. 各模块独立计算，没有真正的"协同"和"取长补短"

### 2.2 已实现的模块

| 模块 | 状态 | 说明 |
|------|------|------|
| GRU Cell | ✅ 完整 | 双层 GRU，含 BPTT |
| SelectiveSSM | ✅ 完整 | Mamba S6 内核 |
| Sparse Attn | ✅ 完整 | 学习型自注意力，窗口 hist |
| CNN | ✅ 完整 | 1D卷积核3，独立预测头 |
| STDP | ✅ 完整 | 突触权重可塑性 |
| STDA | ✅ 完整 | 阈值适应 |
| Titans | ✅ 完整 | 在线神经记忆 |
| MoE | ❌ 空 | registry 有条目，无实现 |
| SNN | ❌ 空 | 被 shelved，无实现 |

---

## 三、目标架构：多神经协同 + MoE

### 3.1 设计目标
1. **多神经独立处理**：每个神经模块独立计算输出
2. **动态路由**：根据任务/上下文选择激活哪些神经
3. **MoE 专家库**：多个专家网络，门控网络选择组合
4. **取长补短**：不同神经擅长不同模式，协同互补

### 3.2 架构图
```
                    ┌─────────────────────────────────────┐
                    │         门控网络 (Gating)            │
                    │   输入: h_emb → 路由权重 [w1..wn]    │
                    └──────────────┬──────────────────────┘
                                   │
          ┌────────────────────────┼────────────────────────┐
          │                        │                        │
          ▼                        ▼                        ▼
   ┌──────────────┐       ┌──────────────┐       ┌──────────────┐
   │  GRU 神经     │       │  SSM/Mamba   │       │  Attn 神经   │
   │  (快动力学)    │       │  (状态流)     │       │  (慢语义)     │
   └──────┬───────┘       └──────┬───────┘       └──────┬───────┘
          │                      │                      │
          ▼                      ▼                      ▼
   ┌──────────────┐       ┌──────────────┐       ┌──────────────┐
   │  CNN 神经     │       │  Titans 神经  │       │  SNN 神经    │
   │  (局部感知)    │       │  (记忆检索)   │       │  (脉冲门控)   │
   └──────┬───────┘       └──────┬───────┘       └──────┬───────┘
          │                      │                      │
          └──────────────────────┼──────────────────────┘
                                 │
                                 ▼
                    ┌────────────────────────┐
                    │     协同输出组合          │
                    │   y = Σ wᵢ · fᵢ(x)     │
                    └────────────────────────┘
                                 │
                                 ▼
                    ┌────────────────────────┐
                    │      预测头 (Vocab)      │
                    └────────────────────────┘
```

### 3.3 MoE 设计
- **专家数量**: 5-7 个（GRU, SSM, Attn, CNN, Titans, SNN）
- **门控网络**: 输入 emb → softmax → 专家权重
- **Top-k 选择**: 每步只激活 k=2-3 个专家（稀疏路由）
- **负载均衡**: 辅助损失防止专家垄断

---

## 四、待完成任务

### 4.1 高优先级

#### [ ] 1. 实现 MoE 核心模块
**文件**: `src/ml/moe.ts`
- `MixtureOfExperts` 类
- 专家注册/管理
- 门控网络
- Top-k 路由
- 辅助负载均衡损失

#### [ ] 2. 重构 CharGRU → CharMultiNeuro
**文件**: `src/ml/multineuro.ts`（新文件）
- 将串行架构改为并行+协同
- 每个神经模块独立计算
- 门控网络动态路由
- 保留旧 CharGRU 兼容性

#### [ ] 3. 更新注册表
**文件**: `src/station/registry.ts`
- MoE 从 shelf 移到 active
- 标记各神经的 roleTag（perception/central/output）
- 添加 compute unit 定义

### 4.2 中优先级

#### [ ] 4. 实现 SNN（可选）
**文件**: `src/ml/snn.ts`
- LIF 神经元
- 脉冲动力学
- 需要决定：是否用 SNN 替换部分连续神经

#### [ ] 5. 训练脚本适配
**文件**: `src/ml/train.ts`
- 支持新的 CharMultiNeuro
- 添加 MoE 相关 CLI 参数
- 保存/加载 MoE 参数

#### [ ] 6. 验证与测试
- 基准测试：对比串行 vs 协同性能
- 消融实验：移除单个神经观察影响
- 可视化：路由权重分布

### 4.3 低优先级

#### [ ] 7. 持久化看门狗
- 修复 `nohup node dist/ml/watchdog.js` 退出问题
- 注册 cron 或 systemd service
- 确保自动启动

#### [ ] 8. 性能优化
- BPTT 截断长度调优
- Batch size 调整
- 内存/swap 监控

---

## 五、关键技术决策

### 5.1 协同 vs 串行
- **串行**: 固定顺序，计算量大但结构简单
- **协同**: 并行计算 + 动态路由，效率更高但实现复杂
- **决策**: 采用协同架构，门控学习何时用哪个神经

### 5.2 MoE 粒度
- **细粒度**: 每个专家是一个小网络
- **粗粒度**: 每个专家是一个完整神经模块
- **决策**: 粗粒度，每个已有模块作为一个专家

### 5.3 路由策略
- **Top-1**: 每次只用一个专家（最稀疏）
- **Top-K**: 每次用 K 个专家（平衡稀疏与融合）
- **决策**: Top-2，允许专家协作

---

## 六、当前训练状态

### 6.1 最近训练结果
- **Epoch**: 4/4
- **Val Loss**: 1.209
- **Top-1 Acc**: 51.8%
- **Recall**: 10.5%
- **参数**: 338,183
- **检查点**: `sandbox/ck-full-multi/`

### 6.2 STDP/STDA 状态
- **已启用**: 是
- **Log 输出**: 需要重启验证（旧编译不含 STDP/STDA）
- **实现**: `stdp.ts` 和 `stda.ts` 已集成到 `train.ts`

---

## 七、下一步行动

### 立即执行
1. 创建 `src/ml/moe.ts` — MoE 核心实现
2. 创建 `src/ml/multineuro.ts` — 多神经协同模型
3. 更新 `src/station/registry.ts` — MoE 激活
4. 编译并验证
5. 重启训练

### 用户确认
- 是否需要实现 SNN？还是先聚焦 MoE + 协同？
- 专家数量：建议 5-6 个（GRU, SSM, Attn, CNN, Titans, MoE-Gate）
- Top-k 值：建议 k=2

---

## 八、注意事项

1. **路径约定**: 所有操作在 `/home/aidlux/tianxuan/` 下执行
2. **编译输出**: `tsc` 输出到 `sandbox/dist/`
3. **内存**: 11GB RAM，8GB swap，注意不要 OOM
4. **看门狗**: 需修复持久运行问题

---

*最后更新: 2026-08-26 21:05*
