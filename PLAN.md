# 天玄 TianXuan — 项目设计方案(已定稿)

## 一句话定位

一个 ≤1000 万参数、≤2000 万 token 的**分级招募式世界模型**:
用「共享底座深度路径(MoD 式)」+「快慢双动力学」+「神经记忆」,让模型在预算内按任务复杂度招募深度与记忆。

## 三条核心决策(用户拍板)

| 决策 | 选项 | 含义 |
|---|---|---|
| L1/L3 物理关系 | **MoD 式共享底座(深度路径)** | 同一模型:按预算选初始深度,低置信在预算内扩深;旧「两套独立网络」已被证据否决(见修订记录) |
| 世界的定义 | **文本世界 + 物理世界(两阶段)** | 先文本世界验证机制;后接简单物理环境(模拟真实物理世界为最终目标) |
| 预算上限 | **≤1000 万参数 / 2000 万 token** | 锁死,CPU 可做,消融可承担 |

---

## 架构全图

```
                    ┌──────────────────────────────────────┐
                    │             分级执行器                 │
                    │        (置信度兜底 + 自洽性校验)        │
                    └──────────────────────────────────────┘
  L0 记忆命中 ── Titans 记忆/缓存 → 直接输出(零算力)
  L1 简单    ── 浅路径(同模型低层) → 快出, 低置信在预算内扩深
  L2 中等    ── 预算内加深: 共享世界状态 z + 快动力学(Mamba 层)
  L3 复杂    ── 更深路径 + 稀疏 Attention 层 + 记忆检索
  L4 极难    ── + 想象滚动(无输入推进 z → 世界推演/规划)

共享底座: char-level tokenizer / embedding / 输出头 / 世界状态 z 接口
```

## 神经系统注册表(可选插拔,每个进组带消融)

| 系统 | 角色 | 阶段 |
|---|---|---|
| Mamba(快动力学) | token 级线性状态流 | 一期 |
| 稀疏 Attention(慢语义) | 事件级语义演化 | 二期 |
| Titans 神经记忆 | 长期记忆 + L0 快通道 | 三期 |
| RWKV(感知快车道) | 低延迟读入 | 后置 |
| KAN(解码增强) | 精准非线性变换 | 后置 |
| MoE(专家库) | 领域专家 | 后置 |
| SNN(事件门控) | 稀疏唤醒(实验) | 后置 |
| STDP(脉冲时序依赖可塑性) | SNN 突触权重学习规则(脉冲时序配对) | 后置 |
| STDA(脉冲时序依赖适应) | SNN 神经元兴奋性/阈值调节 | 后置 |
| LNN(液态时间常数网络) | 连续时间状态动力学 | 后置 |

## 证据审核修订(2026-07)

| 变更 | 依据 | 现状 |
|---|---|---|
| 双网否决 → **MoD 式深度路由** | DeepMind《Mixture-of-Depths》arXiv:2404.02258 | 同一模型,预算内按深度动态分配计算 |
| MoE 否决 | Switch Transformers(arXiv:2101.03961)以超大稀疏参数为前提 | 货架,不进主线 |
| SNN/STDP/STDA 移入货架 | Surrogate Gradient(arXiv:1901.09948):收益绑定神经硬件、训练更难;STDA 无文献术语 | registry.status="shelf" |
| LNN 移入货架 | Liquid Time-constant Networks(arXiv:2006.04439):连续时间 RNN/神经 ODE 类,与 Mamba/SSM 动力学重叠 | registry.status="shelf",出现状态建模可测缺口才评估 |
| Titans + L0 优先级最高 | Titans(arXiv:2501.00663)官方定位一致 | 主实验 |
| 数据预算重标定 | Chinchilla(arXiv:2203.15556)约 20 token/参数 | 2000 万 token 按「超小量」口径评估 |
| RWKV/KAN 降为候选 | 论文动机=长上下文/低延迟优势 | 出现可测瓶颈才允许启用 |

**落地规则**: 每个系统进站登记必须带 `justification`(修复基线哪个指标缺口),没有证据不允许 `enabled`。

## 全局工作站 Global Station(中枢枢纽)

所有神经系统注册进同一个工作站,通信只走黑板,状态由工作站统一托管。

- 注册中心 Registry:系统进站登记(名称/角色/阶段/启用状态/深度),可插拔,进组必须带消融标记 + justification 门槛
- 全局黑板 Blackboard:唯一共享世界状态 z(普通对象读写,不做消息总线)
- 分级执行器 Executor:MoD 式——预算内选初始深度,低置信扩展;L0 记忆命中零算力直出;中间状态写回 z 复用
- 健康监控 Monitor:心跳/延迟/占用/NaN 检测,异常系统自动降级或摘除(骨架期以状态字段占位)
- 审计日志 Audit:每次路由的决策链(招募了谁/置信度/耗时)全量记录,可回放

## 世界: 两阶段

- v1 文本世界:合成事件时序语料(程序生成,可标注)+ 真实中文故事/新闻/历史(公有领域/开源许可)
- v2 物理世界:接入简单物理模拟器(如刚体/简单动力学),状态序列作为训练数据 → 终极目标真实物理世界

---

## 已识别的风险与对策(三轮审核汇总)

# 硬伤
1. SNN 低功耗在 CPU 上不成立 → 降级为后置实验,默认关闭
2. 多系统协同小模型收益低 → 有一整套基线对照组,每个系统必配消融
3. 异质系统训练收敛/路由塌缩 → 分阶段冻结训练 + 路由熵正则
4. 系统间状态转换有损 → 统一共享世界状态 z,通信走黑板,零互转
5. tfjs-node 生态(内核缺失) → 朴素循环实现;第 0 步技术验证不过就换 Python
6. 想象无训练信号 → 未来状态预测损失 + 重建损失兜底

# 遗漏
7. 评估体系 → 困惑度/回忆/长上下文/延迟/能耗 + 世界专属指标(事件预测/时序一致性/反事实)
8. 数据管线 → 双轨:合成 + 真实,合规审查
9. 基线对照组 → 纯 Transformer + 纯 Mamba,必须同配置

# 执行层
- 置信度校准不可靠 → 自洽性采样(多人候选一致)+ 验证集离线条次
- 中文 tokenizer → 定 char-level(字符级,小词表)
- 可复现三件套 → 固定种子/数据切分/配置快照
- 数值自检 → 梯度检查 / shape 校验 / NaN 检测
- L0 缓存误命中 → 命中即校验 + 失效机制
- git 损坏 → 已备份并重建


## 里程碑
- 第 1 阶段: 基线(纯 Transformer + 纯 Mamba) + 评估 + 数据管线,出数字
- 第 2 阶段: 世界循环 + 快慢双动力学 + 未来状态损失(消融对比基线)
- 第 3 阶段: 记忆 + L0-L4 分级(延迟/算力可测)
  - 第 3 阶段记忆模块同构于 BAAI 记忆/推理解耦方案(小模型推理 + 外部向量记忆,换库即换领域),已内建于 vectorstore.ts + titans.ts + executor L0 直出
- 第 4 阶段: 想象滚动 + 后置系统逐个消融

## 产品形态(2026-07)

骨架已升级为**可运行产品 MVP**(非演示):

- `src/ml/` — 纯 TS 数值内核(零原生依赖): 字符分词器 / 数据管线 / 双层 GRU 序列模型(截断 BPTT) / Adam 训练 / 检查点
- `data/` — 真实公版中文语料(三国志演义、水浒传,可扩展) + 合成兜底;Gutenberg 头尾自动剥离
- `src/ml/vectorstore.ts` — 纯 TS 向量库(模型嵌入+余弦检索+持久化);`npm run ingest` 建库,生成时支持 L0 记忆直出与测试时记忆回写
- `npm run train` — 训练并保存 data/checkpoints(训练量/参数可配;验证损失与 top-1 准确率)
- `npm run generate` — CLI 生成,真实走工作站 MoD 路由(记忆直出 / 浅路径 / 深路径)
- `npm start` — Express Web 产品 + 前端控制台(public/),POST /api/generate、GET /api/retrieve(向量检索)
- tfjs-node 原生绑定不可用时已自研 CPU 内核绕过(见 README)

## 实测基线(2026-08, 序列模型改造)

MLP → 双层 GRU 改造完成,同机 CPU、同一语料管线出数字:

| 模型 | 语料 | 参数 | 验证 loss | top-1 | 备注 |
|---|---|---:|---:|---:|---|
| MLP(对照基线) | 15 万×3 轮 | 30.5 万 | 3.10 | 47.7% | 无时序状态;评估以合成语料为主(易) |
| 双层 GRU | 15 万×4 轮 | 76.2 万 | 1.177 | 54.5% | BPTT 32;同上(易) |
| 双层 GRU | 30 万真实×4 轮 | 76.2 万 | 3.437 | 36.0% | 100% 古典小说(三国+水浒),评估更难;perplexity≈31 |
| 双层 GRU(当前) | 30 万真实×8 轮 | 76.2 万 | 3.413 | 35.7% | `--resume` 续训 4 轮;最佳 epoch7 3.361/36.4% |

证据: GRU(Cho et al. arXiv:1406.1078)以循环状态流建模 token 序列,相对无状态 MLP 的增益在文本建模基准上已广泛验证;Titans(arXiv:2501.00663)与 MoD(arXiv:2404.02258)继续按原方案挂载在推理侧(工作站路由 + L0 记忆)。

训练后处理:
- 语料清洗 `cleanCJK`(剔除 ASCII/Gutenberg 内嵌注记)→ 重训后 UNK 压力从 10.5% 降到 ~0
- 推理侧 UNK 槽禁出(末位 `\uFFFD` 概率置 0 后重归一)
- 向量库以 GRU `embedAvg` 重建(旧库为 MLP 嵌入,已随 `npm run ingest` 覆盖)

数据扩充(已完成): `scripts/fetch-zh-classics.sh` 从 Project Gutenberg 拉取 11 部全本公版古典名著,
清洗后合计约 548 万字符(红楼梦 85 万 / 西游记 72 万 / 史记 63 万 / 隋唐演义 61 万 / 三国 59 万 / 封神 59 万 / 水浒 52 万 / 聊斋 48 万 / 儒林外史 27 万 / 东周 15 万 / 世说 8 万)。

下一步(证据驱动): 当前 300k×8 已到该语料上限(平台期);548 万字符全量训练仍低于 Chinchilla 最优
(0.76M 参数 ≈ 1500 万 token,548 万约为 1/3)。建议:
`npm run train -- --tokens 5000000 --rawperfile 600000 --epochs 2 --lr 0.002`
(每轮约 7-10 小时,检查点每轮保存;预期真实古典文本 loss 降 1+、top-1 到 45%+)

---

## 借鉴原则(2026-08 用户拍板)

- **只借逻辑,不抄代码**: 官方论文/开源项目只作为「机制如何形成」的依据与验证思路;所有实现用天玄自研数值内核,不引入任何外部模型/库依赖。
- **引用的官方来源**(2026 新证据):
  - GPT-5 System Card (arXiv:2601.03267): 统一系统 = 快模型 + 深推理模型 + 实时路由器(按任务类型/复杂度/意图选模型, 路由器持续用真实信号学习) → 天玄任务级路由的依据
  - The Ignition Index (arXiv:2608.05160): 逐层探针拟合点火陡峭度, 量化「全或无点火」;实证 transformer 点火 > SSM(Mamba 近线性=无全局广播) → 天玄「多神经协同是否真形成」的验证指标(自研实现, 仅借鉴思路)
  - "Theater of Mind" for LLMs (arXiv:2604.08206): GWT 认知架构;批评静态记忆池+被动消息传递 → 认知停滞 → 天玄 z 走黑板/主动状态流的对照
  - Worldscape-MoE (arXiv:2607.03964): 异构动作接口专家 + 共享世界动力学底座 → 天玄「注册表专家 + 共享 z 底座」同构佐证
  - Anthropic Global Workspace (arXiv:2607.15495): J-space 结构签名(中间层带/容量/广播) → 协同验证的结构性标准
  - Liquid Time-constant Networks (arXiv:2006.04439): 神经元膜时间常数由输入动态调制, 连续时间状态动力学 → 天玄 LNN 货架候选的官方出处, 与 Mamba/SSM 快动力学重叠, 待状态建模缺口出现再评估
  - 记忆/推理解耦(BAAI 智源):基础小模型只负责推理,参数化外部记忆模块(Faiss 向量索引)管长期知识;410M+6.9B 打平 12B 基准;领域专精只需换记忆模块 → 天玄第 3 阶段「向量库记忆 + L0 直出」同构出处;来源:北京智源人工智能研究院,论文待补链接
- **验证沙箱**: 所有验证环节(编译/冒烟训练/生成测试/实验)一律在 `sandbox/` 内完成, 不触碰 `data/` 生产语料/checkpoint/向量库; 入口 `scripts/sandbox-verify.sh`。

---

## 进度快照(2026-08-11, 多神经协同阶段)

- **GRU 内核修复(重大)**: 候选态前向漏乘重置门 r → 前后向不一致; 已修并梯度检查全绿
  (T=2..9, attnQ/K/V/GW/GB/cell1/cell2/emb 全部 relErr=0.0000)。修复后事件语料基线
  48.5%→49.1%, 并补齐 cell1 循环梯度覆盖、attention 跨时刻 hist 反向与 RMS 雅可比。
- **Attention 改层内调制**(x2 = h1 + gate·context): 旧输出端旁路崩塌(验证 top-1 0.9%)
  已消除, 现与基线持平(47.8% vs 49.1%); gate 学会选择性(双峰分布), 无收益时能全关。
- **证据门槛未过**: recall 任务(AssocCorpus/RecallCorpus)在当前规模下两模型均 ~随机,
  点火探针显示 attention 能定位句首目标但取值解码不足 → attention 保持 shelf, 不 enabled。
- **下一步(按序)**: ① Mamba 真选择性层(快动力学) → ② Titans 在线神经记忆(L0)
  → ③ CNN 消融 → ④ 多神经全上线后才接 548 万字符语料预训练。每个神经按证据门槛进注册表。

---

## 训练工程加固(2026-08-14)

- **批级续训(用户拍板)**: 检查点保存 `lastBatch` + Adam 累计步数 `t`; resume 从断点批继续, 不再重跑轮首段; 系统冻结/被杀后由看门狗 `scripts/watch-train.sh` 自动 `kill+resume`(日志超 1 小时判定冻结), 损失 ≤250 批。
- **原子检查点**: `model.json.tmp` → rename 原子落盘, kill 不会写坏模型文件; `saveCheckpoint` 自动建目录。
- **实测**: 沙箱断点续训验证通过(注入 lastBatch=35 → resume 从批 36 继续); 冻结自愈 3 次自动触发(process-dead ×1, frozen ×2)。
- 防复发: `daily-check.sh` 15/15(新增 lastBatch/原子写两项)。

## 多神经协同落地快照(2026-08-12)

**真神经已进代码(非 stub, 全部经沙箱梯度检查 relErr=0.0000)**
- Mamba 真选择性层 `src/ml/mamba.ts`: Δ/B/C 输入依赖的选择性扫描, 深度 2 替代 GRU cell2 (`--mamba`), 事件语料 30k×2ep top-1 47.5%(GRU 基线 49.1%); SSM+attn 4ep 48.0%。
- Titans 在线神经记忆 `src/ml/titans.ts`: 持久记忆(动量)+深记忆槽(相似度检索), 生成期在线写入, 占用率/命中率可探针。
- GRU/Attention/CNN: 真实内核已在 `src/ml/gru.ts`(Attention 修复见诊断报告 2026-08-11)。
- **CNN 消融(2026-08-12, 证据过门槛)**: `train.ts` 新增 `--cnn 1` 开关; `sandbox/gradcheck-cnn.js` 全 relErr=0.0000; 同配置事件语料 30k×4ep 消融: 基线 top-1 47.5% → +CNN 49.3%(+1.8pt, 修复缺口), loss 持平(1.283→1.286), 就后召回探针 9.7→6.5(小规模代价, 大语料复查)。注册表 CNN evidence.status="pass" → 生产预训练带 `--cnn 1`。
- SNN/STDP/STDA: 按宪章保持货架(注册表带 evidence=fail, 不 enabled)。

**工作站协同机制(真实, 沙箱验证)**
- 注册表: 神经带 `roleTag`(感知/中央/输出) + `evidence`(指标/基线/结果/状态)。
- 神经链: 感知(GRU L1) → 中央(快动力学 SSM/GRU L2 → 慢语义 Attention L3) → 感知(CNN L4), 只经黑板 z 协同, 神经间不直接互调。
- 协同探针 `src/station/probe.ts`(Ignition Index 思路): 记录每单元点火(attention=gate, SSM=Δ均值, CNN=激活均值); 实测 attention 点火方差 0.012 > SSM 0.004(符合"transformer 点火、SSM 近线性"论文实证)。
- 预算转向(根据多神经实际点火调整): 中央神经滑窗点火 < 阈值 → 该深度让路, 审计记 `cold-central`; 沙箱确定性测试 12 任务中 9 次正确跳过。
- 修复执行器级联 bug: 运行 L1 后 targetDepth 应指向 `next.depth+1`, 原 `+=1` 会跳过深度 2。

**证据门槛现状**: GRU(基线状态流, 无此层无输出)/CNN(已过门槛, top-1 +1.8pt)之外, Mamba/Attention/Titans 仍为 evidence=pending(机制正确、不损害基线, 但暂无"修复某指标缺口"的量化提升证据); 按宪章未 enabled 进主线, 待语料/容量升级后补证据。
