# 天玄项目 — 当前状态报告

**更新时间**: 2026-08-30 21:45
**编译状态**: ✅ 0 TypeScript 错误

---

## 一、今天做了什么（6项修复）

| # | 问题 | 修复内容 | 状态 |
|---|---|---|---|
| 1 | DataPurifier 误杀正常中文 | `maxBigramRepeat` 0.8→0.6，古典套语不再被拦截 | ✅ |
| 2 | Executor 有两套实现 | 删除 index.ts 内嵌版，统一到 executor.ts | ✅ |
| 3 | MoE depth 重复前向 | 改为串行：depth=1→2→3→4，每层复用前一层输出 | ✅ |
| 4 | Titans L0 直出不通 | write(x,char) 内部存 chars，删掉外部 Map | ✅ |
| 5 | STDP 完全禁用 | 作为正则项加入 loss，不再直接改 BPTT 梯度 | ✅ |
| 6 | RL 系统孤立 | 新增 `npm run train:rl`，路径统一为 sandbox/dist/ | ✅ |

**验证命令**:
```bash
npm run build              # ✅
npm run train -- --tokens 1000  # ✅ loss 收敛，top-1=7.7%
npm run generate           # ✅ L0:73 路由正常
npm run train:rl           # ✅ 训练正常
```

---

## 二、今天尝试做但没做完的：CharTransformer 接入

### 训练侧 ✅ 已完成
- `train.ts` 新增 `--transformer 1` 开关
- `--nlayers`/`--nhead` 控制层数和头数
- 跑通：`npm run train -- --transformer 1 --nlayers 2 --nhead 2 --emb 64`
- 已保存 checkpoint 到 `sandbox/data/checkpoints-tiny/`

### 生成侧 ❌ 阻塞
- `engine.ts` 加载 Transformer checkpoint 时报错：
  ```
  TypeError: Cannot read properties of undefined (reading 'attn')
      at CharTransformer.backwardBlock
  ```
- 根因：`backwardBlock` 里缓存访问逻辑和前向存储结构不匹配
- 已尝试 3 轮修复，每次修一个 bug 出两个新 bug
- **判断**：继续深跳会偏离主线，暂停。

### 代码写到哪了

**已修改的文件**（本次）:
```
src/ml/data_purifier.ts     — bigram 阈值 0.6
src/station/executor.ts     — 统一配置接口
src/station/index.ts        — 删除内嵌 Executor，改为 import
src/ml/multineuro.ts        — MoE 串行深度 + STDP 正则项
src/ml/titans.ts            — write(x, char) 增加字符参数
src/ml/engine.ts            — L0 直出打通 + Transformer 加载分支（未完成）
src/ml/train.ts             — --transformer 开关 + Transformer 训练路径（完成）
package.json                — npm run train:rl + 路径修正
src/ml/train_rl.ts          — pos 初始化修复
```

**未完成的代码**:
- `engine.ts` 的 Transformer checkpoint 加载分支写完但功能不通
- `transformer.ts` 的 `backwardBlock` 缓存存取逻辑需重构

---

## 三、项目整体架构状态

```
数据层
  data/raw/  11部古典名著 548万字  ✅ 已就绪，但从未用于训练

骨干模型
  GRU (CharMultiNeuro)         ✅ 主线，已训练验证
  Transformer (CharTransformer) ⚠️ 训练通，生成不通

多神经系统
  MoE 路由                     ✅ 修复后串行共享
  Titans 记忆                  ✅ L0 直出打通
  重放缓冲区                   ✅ 已接入
  多巴胺调制                   ✅ 已接入
  STDP/STDA                    ⚠️ 作为正则项，未实测效果

全局工作站
  Registry + Blackboard        ✅
  Executor (MoD 分级)          ✅ 统一实现
  Probe + Audit                ✅

防御系统
  DataCleaner V4               ✅ bigram 阈值已放宽

强化学习
  ActorCritic (PPO-Clip+GAE)   ✅ 代码完整
  train_rl.ts                  ✅ 脚本已接入
  TextEnv reward               ⚠️ 伪嵌入 + 二元 reward，质量低
  TextReward 综合奖励           ❌ 写了但未调用
```

---

## 四、当前能跑的 vs 不能跑的

| 命令 | 状态 |
|---|---|
| `npm run train -- --tokens 1000 --epochs 1` | ✅ GRU 训练正常 |
| `npm run generate --checkpoint data/checkpoints` | ✅ 生成正常 |
| `npm run train:rl -- --steps 100` | ✅ RL 训练正常 |
| `npm run train -- --transformer 1 --nlayers 2` | ✅ Transformer 训练正常 |
| `npm run generate --checkpoint checkpoints-tiny` | ❌ 加载报错 |
| 548 万字真实语料训练 | ⬜ 未执行 |

---

## 五、下一步建议

按优先级排序：

1. **🔴 P0**: 用真实语料训一次 GRU（`--rawperfile 600000 --tokens 5000000`），这是项目价值的关键一步
2. **🟡 P1**: 决定是否花精力修 Transformer 生成侧，还是另写一个干净的 `engine.ts` Transformer 分支
3. **🟡 P2**: RL 的 reward 函数完善（接 `TextReward`，用 GRU embedding 代替伪嵌入）
4. **🟢 P3**: STDP 正则项实测效果（`--spikeThreshold 0.1`）

---

*报告生成时间: 2026-08-30 21:45*
